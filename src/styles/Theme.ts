export type ThemeProps = {
  colors: {
    primary: string;
    secondary: string;
  };
};

const theme: ThemeProps = {
  colors: {
    primary: '#0B0340',
    secondary: '#F23869',
  },
};

export default theme;
